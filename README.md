# FastKanban 📊

Легкая и быстрая Kanban-доска, написанная на Python (FastAPI) с использованием HTMX для интерактивности. Без React, без Node.js, без переусложнения.

## 🚀 Особенности

- **Стек:** FastAPI, SQLModel (SQLite), Jinja2, HTMX, TailwindCSS.
- **KISS & YAGNI:** Серверный рендеринг HTML. Минимум JavaScript.
- **Интерактивность:** Создание колонок, задач, inline-редактирование, удаление, архивация.
- **AI-помощник:** Интеграция с LLM (Gemini) для генерации идей и декомпозиции задач.

## 🛠 Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/ВАШ_НИКНЕЙМ/fastkanban.git
cd fastkanban
```

### 2. Создайте виртуальное окружение

**Windows:**
```powershell
python -m venv venv
venv\Scripts\activate
```

**macOS / Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте конфигурацию (.env)
В проекте используется файл `.env` для хранения секретов (например, ключа API).
Создайте его на основе примера:

```bash
# Скопируйте пример
cp .env.example .env
```

Откройте файл `.env` и укажите URL вашего сервиса генерации (если есть):
```env
GEMINI_API_URL=https://api.example.com/generate
```

### 5. Запустите сервер
База данных `kanban.db` создастся автоматически при первом запуске.

```bash
# Запуск через Python
python main.py

# ИЛИ через Uvicorn напрямую
uvicorn main:app --reload
```

Откройте браузер по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)

## 🤝 Вклад в проект (Contributing)
1. Форкните репозиторий.
2. Создайте ветку (`git checkout -b feature/amazing-feature`).
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`).
4. Запушьте ветку (`git push origin feature/amazing-feature`).
5. Откройте Pull Request.

---
License: MIT